@model RoutingLookup.Models.Search
@if (Model == null) 
{
	// do nothing?
} 
else if (String.IsNullOrEmpty(Model.RoutingNumber))
{
	Html.RenderPartial("_SearchAdvanced", Model);
} 
else 
{
	Html.RenderPartial("_SearchBasic", Model);
}
